# Pins below reflect the environment this project is ACTUALLY developed and
# run against, captured 2026-08-24. The previous version of this file pinned a
# different version of every single package (numpy 1.26.4, torch 2.3.1,
# pandas 2.2.2, ...) and had never been installed from -- the working
# environment was assembled ad hoc and had drifted entirely away from it.
# That mattered: the code is written for the REAL versions, e.g.
# training/classical_features.py carries an np.trapezoid shim precisely
# because np.trapz was removed in NumPy 2.0, which only makes sense against
# NumPy 2.x, not the 1.26.4 this file used to claim.
#
# torch is CPU-only on this machine (no NVIDIA GPU). Install it from the
# PyTorch CPU index BEFORE this file, or pip will pull the much larger
# CUDA build:
#   py -m pip install torch --index-url https://download.pytorch.org/whl/cpu

# Signal processing / data pipeline
mne==1.12.1
numpy==2.4.4
scipy==1.17.1
PyWavelets==1.9.0
pandas==3.0.2
matplotlib==3.11.1
scikit-learn==1.8.0
mne-connectivity==0.8.1

# ML
# ultralytics 8.3.253, NOT the 8.2.31 previously pinned. 8.2.31 calls
# torch.load() without weights_only=False, and PyTorch 2.6 flipped that
# default to True -- so 8.2.31 cannot load ANY pretrained checkpoint on
# torch >= 2.6. Downgrading torch is not an escape: the oldest torch with
# Windows/cp313 wheels is 2.6.0, i.e. the first version with the new default.
# 8.3.x routes through a torch_load wrapper that handles it.
# NOTE: upgrading ultralytics invalidates training/train_yolo_cls.py's
# DISABLE_AUGMENTATION list -- crop_fraction was valid in 8.2.31 and REMOVED
# by 8.3.x, where passing it raises. See the comment on that constant.
ultralytics==8.3.253
torch==2.13.0
torchvision==0.28.0
xgboost==3.2.0

# Backend API
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
httpx==0.28.1

# Cloud (Phase 5) -- not installed locally yet, needed only for deployment
boto3==1.34.131
python-dotenv==1.2.2
